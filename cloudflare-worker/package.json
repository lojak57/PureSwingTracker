{
  "name": "pure-golf-r2-proxy",
  "version": "1.0.0",
  "description": "Cloudflare Worker to proxy uploads to R2",
  "main": "src/index.ts",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
} 